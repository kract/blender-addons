# Blender extension manifest for Keymap Toolkit
# Provides centralized keymap tweaks such as Alt+LMB drag select gestures

schema_version = "1.0.0"

id = "keymap_toolkit"
name = "Keymap Toolkit"
tagline = "Central hub for custom Blender keymaps"
version = "1.1.1"
type = "add-on"

maintainer = "KRACT"
license = ["SPDX:GPL-3.0-or-later"]

blender_version_min = "4.2.0"

website = "https://github.com/kract/blender-addons/tree/main/keymap-toolkit"
tags = ["3D View", "Modeling", "User Interface"]

[permissions]
# No special permissions required for this add-on

[build]
optimized_size = true
